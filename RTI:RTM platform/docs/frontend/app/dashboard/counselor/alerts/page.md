# alerts/page.tsx

Alert list page server component. Fetches alerts with filtering by status/severity. Renders filterable, sortable alert list with pagination.

**Used by:** `/dashboard/counselor/alerts` route
