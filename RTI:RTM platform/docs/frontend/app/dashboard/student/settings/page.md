# settings/page.tsx

Student settings page with account information display and password change option. Fetches user profile data and renders settings cards.

**Used by:** `/dashboard/student/settings` route
