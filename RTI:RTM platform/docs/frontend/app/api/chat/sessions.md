# chat/sessions/route.ts

Chat session management endpoint. GET lists user's sessions, POST creates new session. Handles session metadata and ordering by recency.

**Used by:** Chat session sidebar, new chat creation
