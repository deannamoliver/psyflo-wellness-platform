# time-server-utils.ts

Server-side time formatting utilities. Similar to `time-utils.ts` but designed for server components where timezone handling may differ. Provides consistent date formatting for server-rendered content.

**Used by:** Server components, email templates, report generation
