# signup-cookie.ts

OTP cookie management for the signup flow. Handles secure storage and retrieval of one-time password verification state during the multi-step signup process. Uses encrypted cookies with `SIGNUP_COOKIE_SECRET`.

**Used by:** Sign-up page actions and verification flow
