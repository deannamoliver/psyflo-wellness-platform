# server-utils.ts

Server-side user utility functions. Provides secure user data access, session validation, and user context retrieval for server components and actions.

**Used by:** Server components, server actions requiring user context
