# button.tsx

Primary button component built on Radix UI with multiple variants (default, destructive, outline, secondary, ghost, link) and sizes (default, sm, lg, icon). Uses `class-variance-authority` for variant management.

**Usage:**
```tsx
<Button variant="destructive" size="sm">Delete</Button>
```
