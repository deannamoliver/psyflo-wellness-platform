# badge.tsx

Badge component for status indicators and labels. Supports multiple variants (default, secondary, destructive, outline) for different semantic meanings.

**Used by:** Status indicators, tags, counts, category labels
