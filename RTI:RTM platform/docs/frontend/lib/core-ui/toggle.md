# toggle.tsx

Toggle button component built on Radix UI Toggle primitive. Provides pressed/unpressed state with visual feedback for binary options.

**Used by:** View mode toggles, filter buttons, toolbar actions
