# use-mobile.ts

React hook for detecting mobile viewport. Returns boolean indicating if current viewport is below mobile breakpoint for responsive behavior.

**Used by:** Responsive components, mobile-specific rendering
