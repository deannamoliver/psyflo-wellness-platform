# types.ts

TypeScript type definitions for the chat module. Defines `Message` (id, role, content), `ExecutionTrace`, and other chat-related interfaces used across the chat feature.

**Used by:** `api.ts`, chat UI components, API routes
