# actions.ts

Server actions for alert management in the counselor dashboard. Exports actions for updating alert status, adding notes, and other alert operations. Uses `revalidatePath()` to refresh UI after mutations.

**Used by:** Counselor alert pages, alert card components
