# schema.ts

Zod validation schemas for alert-related forms and data. Defines schemas for alert creation, updates, note addition, and status changes. Used for server-side validation in actions.

**Used by:** `actions.ts`, alert form components
