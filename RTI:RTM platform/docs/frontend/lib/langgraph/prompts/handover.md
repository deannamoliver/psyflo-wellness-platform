# prompts/handover.ts

Prompt for high-risk conversation termination. Exports `HANDOVER_PROMPT` which generates a compassionate message informing the student they're being connected to a wellness coach when `shouldShutdown` is triggered.

**Used by:** `main-graph.ts` (`handoverToCoachNode`)
