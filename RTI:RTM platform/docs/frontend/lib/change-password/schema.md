# schema.ts

Zod validation schema for password change form. Validates current password, new password strength requirements, and password confirmation matching.

**Used by:** `form.tsx`, `action.ts` for form validation
