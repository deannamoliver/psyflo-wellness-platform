# string-utils.ts

String manipulation utilities including truncation, capitalization, slugification, and text formatting helpers. Provides consistent string operations across the codebase.

**Used by:** Display components, URL generation, text formatting
