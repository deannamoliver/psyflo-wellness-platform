# streak.ts

Check-in streak tracking logic. Calculates current streak length, determines if streak is active, and handles streak reset conditions based on missed days.

**Used by:** Dashboard streak display, check-in completion feedback
