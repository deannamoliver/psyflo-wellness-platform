# error.tsx

Error display component for showing error states. Displays error message, optional details, and retry action with appropriate styling and iconography.

**Used by:** Error boundaries, failed data fetches, form submission errors
