# ai/message.tsx

Individual chat message component. Renders user or assistant messages with appropriate styling, timestamps, and optional metadata. Handles markdown rendering and special message types.

**Used by:** `conversation.tsx`, individual message displays
