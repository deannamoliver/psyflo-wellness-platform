# chat/route.ts

Main chat API endpoint handling message submission. Authenticates user, processes message through LangGraph, and returns AI response. Supports both sync and streaming modes.

**Used by:** Chat interface message submission
