# profile/page.tsx

Profile setup hub showing onboarding step progress. Lists completed and pending steps with navigation. Redirects to dashboard when all required steps complete.

**Used by:** `/onboarding/student/profile` route
