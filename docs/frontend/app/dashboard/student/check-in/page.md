# check-in/page.tsx

Daily mood check-in page server component. Fetches today's check-in status and renders emotion picker. Prevents duplicate check-ins if already completed.

**Used by:** `/dashboard/student/check-in` route
