# chat/page.tsx

Chat listing page that redirects to most recent session or creates new one. Serves as entry point for chat feature with session management.

**Used by:** `/dashboard/student/chat` route
