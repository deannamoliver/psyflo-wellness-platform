# login/page.tsx

Login page component with email input and OTP verification. Renders two-step form: email entry, then OTP code input. Redirects to appropriate dashboard on success.

**Used by:** `/login` route
