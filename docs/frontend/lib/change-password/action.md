# action.ts

Server action for password change. Validates current password, updates via Supabase Auth, and returns success/error status. Handles authentication errors gracefully.

**Used by:** Password change form submission
