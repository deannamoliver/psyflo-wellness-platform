# prompts/system.ts

System prompts for the base conversational agent. Exports `getBaseAgentSystemPrompt(age?)` which returns an age-appropriate system prompt for emotional support conversations, and `getConversationalWrapperPrompt()` for natural response generation.

**Used by:** `base-agent.ts`
