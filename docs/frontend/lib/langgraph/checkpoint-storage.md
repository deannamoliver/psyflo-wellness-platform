# checkpoint-storage.ts

Low-level checkpoint storage operations for direct database access to checkpoint data. Used for debugging and admin trace visualization rather than normal conversation flow.

**Interacts with:** PostgreSQL checkpoint tables (`checkpoints`, `checkpoint_blobs`)
