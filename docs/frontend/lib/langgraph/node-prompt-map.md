# node-prompt-map.ts

Maps graph node names to their corresponding prompt templates for the admin UI. Used to display the actual prompts used at each step when viewing execution traces in `/admin/agents/[sessionId]`.

**Interacts with:** `prompts/` directory, used by admin UI components
