# checkpoint-diff.ts

Utilities for computing differences between checkpoint states. Used by the admin UI to show state changes between graph execution steps.

**Interacts with:** `checkpoint-storage.ts`, `trace-history.ts`
