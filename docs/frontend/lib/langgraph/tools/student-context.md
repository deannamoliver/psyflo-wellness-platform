# tools/student-context.ts

Retrieves student context information for personalizing AI responses. Exports `getStudentBirthday(userId)` to fetch student's birthday for age calculation, enabling age-appropriate conversation tone in base agent.

**Used by:** `base-agent.ts` for age-appropriate system prompts
