# pagination.tsx

Table pagination component with page navigation, page size selection, and item count display. Integrates with server-side pagination for large datasets.

**Used by:** Paginated tables throughout counselor and admin dashboards
