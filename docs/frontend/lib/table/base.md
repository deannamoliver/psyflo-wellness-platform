# base.tsx

Base table component built on shadcn/ui table primitives. Provides consistent table styling, sortable headers, and responsive behavior for data displays.

**Used by:** Student lists, alert tables, admin data views
