# app.ts

Application configuration constants including app name, default metadata, and environment-specific settings. Provides centralized configuration values used throughout the frontend.

**Used by:** Layout components, metadata generation, SEO configuration
