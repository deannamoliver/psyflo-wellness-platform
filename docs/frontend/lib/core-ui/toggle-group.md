# toggle-group.tsx

Toggle group component built on Radix UI ToggleGroup primitive. Provides single or multi-selection toggle buttons in a group with consistent styling.

**Used by:** View mode selection, filter groups, segmented controls
