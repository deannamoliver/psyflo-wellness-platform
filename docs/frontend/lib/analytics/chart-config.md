# chart-config.ts

Recharts configuration for analytics visualizations. Defines color schemes, axis formatting, tooltip styles, and responsive breakpoints for consistent chart appearance across dashboards.

**Used by:** Counselor analytics, student progress charts, mood trends
