# actions.ts

Server actions for email notification preferences. Handles updating email filter settings for alert types, frequency preferences, and notification thresholds.

**Used by:** Email filter manager components
