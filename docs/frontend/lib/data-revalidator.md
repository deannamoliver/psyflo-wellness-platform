# data-revalidator.tsx

Client component that triggers cache revalidation on mount. Used to refresh server-cached data when returning to a page, ensuring users see updated information.

**Used by:** Dashboard pages, lists that need fresh data on navigation
