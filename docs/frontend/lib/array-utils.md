# array-utils.ts

Array manipulation utilities including grouping, filtering, sorting, and transformation helpers. Provides type-safe array operations used throughout the application.

**Used by:** Data processing, list components, aggregation logic
